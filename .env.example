# Copy to frontend/.env.local and set the correct URL

# Point at mock server during development:
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Switch to real backend at integration time — same value if running locally:
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
